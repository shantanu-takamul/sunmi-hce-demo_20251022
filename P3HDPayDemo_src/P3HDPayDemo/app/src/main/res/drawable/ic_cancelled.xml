<?xml version="1.0" encoding="utf-8"?>
<!--
  Void mark for a cancelled sale. Deliberately the same weight, cap and stroke as
  ic_check so the two hero states read as one family and only the meaning changes.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="36dp"
    android:height="36dp"
    android:viewportWidth="24"
    android:viewportHeight="24">
    <path
        android:fillColor="@android:color/transparent"
        android:pathData="M6,6L18,18"
        android:strokeColor="@color/error"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:strokeWidth="2.5" />
    <path
        android:fillColor="@android:color/transparent"
        android:pathData="M18,6L6,18"
        android:strokeColor="@color/error"
        android:strokeLineCap="round"
        android:strokeLineJoin="round"
        android:strokeWidth="2.5" />
</vector>
